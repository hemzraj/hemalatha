#include <stdio.h>

void main()

{

    int x,y,num;
    printf("enter the x,y value");
    scanf("%d%d",&x,&y);
    {
        for(i=x;i<=y,i++)
        {
            if(i%2==1)
            printf("print the i value%d",i);
        }
    }

}
